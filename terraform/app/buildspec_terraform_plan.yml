version: 0.2
phases:
  pre_build:
    commands:
      - "cd terraform/app"
      - "terraform init"
  build:
    commands:
      - "cd terraform/app"
      - "terraform plan -out=./.tfplan"

artifacts:
  base-directory: ${CODEBUILD_SRC_DIR}
  files:
    - "**/*"